# a method that takes an array of integers, 
# then returns a new array with every other element from the original array, 
# starting with the first element. 

=begin 

Given an array of integers.

To get only every other element of the original array, iterate over the elements of original array
  -create new array to contain results
  -add to that array all elements from original array with an index of 0 or evenly divisible by 2

Print new array.

=end